cache_directories:
­ ~/.android
­ ~/.gradle
override:
­ ./gradlew dependencies

test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;

general:
  artifacts:
    - /home/ubuntu/DBFlowTest/app/build/outputs/apk/

deployment:
  develop:
    branch: develop
    commands:
      - sudo bash ./deploy_tohockey.sh

